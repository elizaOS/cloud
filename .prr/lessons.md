# PRR Lessons Learned

> This file is auto-generated by [prr](https://github.com/elizaOS/prr).
> It contains lessons learned from PR review fixes to help improve future fix attempts.
> You can edit this file manually or let prr update it.
> To share lessons across your team, commit this file to your repo.

## File-Specific Lessons

### app/api/auth/siwe/verify/route.ts

- Fix for app/api/auth/siwe/verify/route.ts:191 - tool modified wrong files (app/api/auth/siwe/nonce/route.ts), need to modify app/api/auth/siwe/verify/route.ts
- Fix for app/api/auth/siwe/verify/route.ts:527 - Review requires new test files with actual test cases (nonce TTL, signature validation, race conditions, etc.) — deleting a comment is not test coverage
- Fix for app/api/auth/siwe/verify/route.ts:527 - tool made no changes without explanation - trying different approach
- Fix for app/api/auth/siwe/verify/route.ts:527 - The review explicitly requested implementing actual test files with coverage, not documentation comments about needing tests.
- Fix for app/api/auth/siwe/verify/route.ts:224 - Ensure to replace the `cache.del()` logic with the `atomicConsume` to correctly check and validate nonce consumption, thereby avoiding the constant failure of nonce validation.
- Fix for app/api/auth/siwe/verify/route.ts:610 - All invalid markup must be entirely removed from the code so that only functional logic remains. Ensure that no extraneous tool output is left in the file.
- Fix for app/api/auth/siwe/verify/route.ts:71 - Ensure that the resolution of keys properly checks `expires_at` against the current date to prevent reusing expired keys, thus ensuring users receive functional API keys during authentication.
- Fix for app/api/auth/siwe/verify/route.ts:602 - Race recovery at lines 592-595 must add `if (!raceUser.is_active || !raceUser.organization?.is_active) return 403 ACCOUNT_INACTIVE` before line 594 — diff doesn't touch this code path at all.

### public/.well-known/llms-full.txt

- Fix for public/.well-known/llms-full.txt:5661 - Only one occurrence of the apiKey prefix was updated (line 5659). The comment references "Also applies to: 5677-5681" indicating a second location that was not addressed in this diff.

### public/llms-full.txt

- Fix for public/llms-full.txt:5673 - The diff shown only updates the apiKey at line 5659. The comment requests standardization across "related samples and tests" but doesn't confirm whether line 5677-5681 or other occurrences were also updated.
- Fix for public/llms-full.txt:5674 - Find and replace the `apiKey: "eliza_..."` value with `apiKey: "ek_test_..."` or `apiKey: "ek_live_..."` on line 5674, not unrelated fields.

### lib/cache/client.ts

- Fix for lib/cache/client.ts:62 - File corruption fix requires restoring ALL orphaned method bodies and removing foreign code spliced into methods — deletion alone leaves incomplete structure
- Fix for lib/cache/client.ts:50 - File must have proper class structure with all methods complete — cannot just remove duplicates without fixing what they were masking (corrupted initialize() and isCircuitOpen())
- Fix for lib/cache/client.ts:424 - Remove the nested `isCircuitOpen` method entirely and clean up the original method to resolve syntax errors and dangling lines.

### lib/utils/signup-helpers.ts

- Fix for lib/utils/signup-helpers.ts:48 - Ensure changes directly address the core issue by importing shared functions instead of just adding comments about duplication.

### app/api/auth/siwe/nonce/route.ts

- Fix for app/api/auth/siwe/nonce/route.ts:85 - RESULT: ALREADY_FIXED — The code already handles the missing `isAvailable` method defensively at lines 51-75.
