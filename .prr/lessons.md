# PRR Lessons Learned

> This file is auto-generated by [prr](https://github.com/elizaOS/prr).
> It contains lessons learned from PR review fixes to help improve future fix attempts.
> You can edit this file manually or let prr update it.
> To share lessons across your team, commit this file to your repo.

## Global Lessons

- Fix for content/api/voice.mdx rejected: The fix updates voiceId examples to UUID format but does not address the core review comment about updating voice ID path parameters to UUID format in GET `/api/v1/voice/{id}` examples (the diff shows only voiceId body parameter changes, not path parameter examples).
- Fix for content/api/voice.mdx rejected: The fix updates voiceId examples to UUID format but fails to address the core review comment about updating voice ID path parameters in the `GET /api/v1/voice/{id}` endpoint and related list/clone sections to use UUID-shaped IDs instead of `voice_xyz789` format.
- fixer made no changes without explanation - trying different approach

## File-Specific Lessons

### content/api/credits.md

- Fix for content/api/credits.mdx:314 - "When fixing API documentation, update both the parameter list AND the example response structure to match the actual implementation."

### content/api/voice.md

- Fix for content/api/voice.mdx:131 rejected: The fix updates voiceId examples to UUID format but does not address the core review comment about the Speech to Text response format mismatch (missing changes to document `transcript` and `duration_ms` fields instead of `text`, `language`, `confidence`).
