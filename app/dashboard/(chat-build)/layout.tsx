/**
 * Chat/Build Shared Layout
 * Simple wrapper - actual layout handled by ElizaPageClient
 */

export default function ChatBuildLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
