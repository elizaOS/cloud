# Sensitive variables — provide via TF_VAR_ env vars or a secrets manager
# Currently empty — Artifact Registry auth is handled natively by GKE.
# Add project-specific secrets here as needed (database URLs, API keys, etc.)
